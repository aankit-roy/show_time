import 'package:flutter/material.dart';

class AppColors{
  // Netflix Red
  static Color primaryRed = const Color(0xFFE50914);

  // Netflix Dark Black Background
  static const Color backgroundBlack = Color(0xFF141414);

  // Dark Gray for text
  static const Color darkGray = Color(0xFF3A3A3A);

  // Light Gray for secondary text and borders
  static const Color lightGray = Color(0xFFB3B3B3);

  // Netflix Dark Gradient
  static const Color darkGradientStart = Color(0xFF141414);
  static const Color darkGradientEnd = Color(0xFF000000);

  // White for titles and highlights
  static const Color white = Color(0xFFFFFFFF);
}